[build]
  # Netlify builds the Jekyll site automatically on every push.
  command = "bundle install && bundle exec jekyll build"
  publish = "_site"

[build.environment]
  RUBY_VERSION = "3.3.0"
  NODE_VERSION = "22"
